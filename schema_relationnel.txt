Ordre = (NumOrdre INT, nomOrdre VARCHAR(50));
Organisme = (SIRET INT, raison_sociale VARCHAR(50));
Grade = (idGrade INT, nomGrade VARCHAR(50));
Rang = (idRang INT, nomRang VARCHAR(50));
Titre = (idTitre INT, nomTitre VARCHAR(50));
Dignite = (idDignite INT, nomDignite VARCHAR(50));
Machine = (numMachine INT, modèle VARCHAR(50), nomMachine VARCHAR(50), certifié BOOLEAN);
Registre = (idRegistre INT, typeEntretien VARCHAR(50), periodicité INT, #numMachine);
Legume = (idLegume INT, nomLegume VARCHAR(50));
Sauce = (idSauce INT, nomSauce VARCHAR(50));
Idoine = (#idDignité);
GradeChevalierDame = (#idGrade);
Adresse = (idAdresse INT, adresseRepas VARCHAR(50));
MachineCertifie = (#numMachine, #(#idDignité));
Aliment = (idAliment INT, nomAliment VARCHAR(50));
Eleveur = (idEleveur INT, nomEleveur VARCHAR(50));
Animal = (idAnimal INT, age BYTE, pays VARCHAR(50), #idEleveur);
Lait = (idLait INT, typeLait VARCHAR(50), pasteurisé BOOLEAN, #idAnimal);
Allergenes = (idAllergenes INT, typesAllergenes VARCHAR(50));
Croyance = (idCroyance INT, nomCroyance VARCHAR(50));
Club = (numClub INT, nomClub VARCHAR(50), #NumOrdre*);
Tenrac = (code INT, nomTenrac VARCHAR(50), courriel VARCHAR(50), adresse VARCHAR(50), numTelephone SMALLINT, #idDignité, #idRang, #idTitre, #SIRET, #idGrade, #numClub, #NumOrdre);
Raclette = (idRaclette INT, typeRaclette VARCHAR(50), #idLait);
Repas = (idRepas INT, nomPlat VARCHAR(50), #idRaclette, #idLegume, #idAliment, #idSauce, #(#idGrade), #(#numMachine));
Enregistre = (#numClub, #NumOrdre, #idRegistre);
se_regroupent = (#numClub, #code, #idRepas, #idAdresse, date_ VARCHAR(50));
connait = (#NumOrdre, #idAdresse);
peut_contenir = (#idLegume, #idSauce, #idAliment, #idAllergenes);
peut_affecter = (#idLegume, #idCroyance);
croit = (#code, #idCroyance);